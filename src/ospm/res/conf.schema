{
    "type": "object",
    "properties": {
        "environment-type" : {"enum": ["development", "validation", "production"]}
    },
    "required": ["environment-type"],
    "additionalProperties": false
}